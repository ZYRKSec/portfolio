# Loose source files at root (not needed for the live site — copies live in assets/)
/*.jpg
/*.jpeg
/*.png
/*.docx

# Tooling / dependencies (not part of the site)
.claude/
node_modules/
package.json
package-lock.json
